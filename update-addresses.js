const app = require('./app.js');

console.log(app)

// db.each("SELECT * FROM addresses", function(err, row) {
//     console.log(row);    // and other columns, if desired
// });

// Address.find({ where: { id: '17146' } })
//   .on('success', function (project) {
//     // Check if record exists in db
//     if (project) {
//       project.update({
//         country: 'France'
//       })
//       .success(function () {})
//     }
//   })
